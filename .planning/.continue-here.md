---
context: phase
phase: 21
phase_name: Chat-to-Canvas Reliability & Conversational UX
task: execute-phase in progress — Waves 1 & 2 complete, Waves 3 & 4 pending
total_tasks: 10
status: paused
last_updated: 2026-05-03T15:45:00Z
---

## Current State

Phase 21 execute-phase is partially complete. **Waves 1 and 2 have been fully implemented.** Waves 3 and 4 are queued and ready to run.

## Completed Work This Session

1. **Wave 1: Plan 01 — Whitelist Reconciliation** — DONE
   - Added missing action types to `src/prompts/shared.js` CANVAS_ACTION_TYPES (zoom_in, zoom_out, set_zoom, reset_view, auto_layout, tidy_canvas, group_selection, ungroup_selection, search_card, export_report, create_card, new_card, open_references, set_thinking_mode, set_deep_think_mode)
   - Replaced hardcoded `CANVAS_TOOL_TYPES` in server.js with import from `src/prompts/shared.js`
   - `VOICE_ACTION_TYPES` now uses `CANVAS_ACTION_TYPES`

2. **Wave 1: Plan 02 — Tool Calling Refactor** — DONE
   - Added `CANVAS_ACTION_TOOL_SCHEMA` and `CANVAS_TOOLS` constants in server.js
   - Added `extractToolCallActions()` helper to parse OpenAI-style tool_calls
   - Updated `handleChat` to use `buildChatSystemContext` + `tools` + `tool_choice: "auto"`
   - Removed `applyJsonObjectResponseMode` from chat endpoint
   - Removed JSON parse fallback from `handleChat` and `buildChatResultFromResponse`
   - Updated `collectStreamingChatPayload` to collect `tool_calls` from streaming deltas
   - Updated `streamChatCompletions` to include `tool_calls` in fake response object
   - Removed length constraints from `src/prompts/chat.js` and `src/prompts/shared.js`
   - Deleted `buildChatActionSystemPrompt` from chat.js, index.js exports, and server.js import
   - Set default `max_tokens: 4096` for chat config
   - Extended `ensureChatFallbackActionsClean` with `FALLBACK_KEYWORDS` covering all 7 rich node types + zoom + reset_view

3. **Wave 2: Plan 03 — Streaming Default** — DONE
   - Main chat now always uses `postStreamingChat` (no more conditional on thinking mode)
   - `pendingAssistant` message is created for all chat modes
   - Added `event: reply` handling in `postStreamingChat` with progressive `replyBuffer` accumulation
   - Updated subagent call to use `postStreamingChat`
   - Zero `postJson` calls to `/api/chat` remain

4. **Wave 2: Plan 04 — Markdown Rendering** — DONE
   - Added DOMPurify CDN script to `public/app.html`
   - Added `import { micromark } from "https://esm.sh/micromark@4"` at top of `public/app.js`
   - Added `renderMarkdownToHtml()` and `addCopyButtons()` helpers
   - Updated `renderChatMessages` to render assistant messages as sanitized markdown HTML
   - Added streaming cursor rendering for pending assistant messages
   - Added translation keys `chat.copyCode` / `chat.copied` in both zh/en
   - Added markdown + streaming cursor CSS to `public/styles.css`

## Execution Queue

| Wave | Plans | Status | What it builds |
|------|-------|--------|----------------|
| 1 | 01, 02 | COMPLETE | Backend: whitelist reconciliation + OpenAI tool calling refactor |
| 2 | 03, 04 | COMPLETE | Frontend: default SSE streaming + micromark/DOMPurify markdown rendering |
| 3 | 05 | Ready | Integration: fallback inference + inline action feedback cards |
| 4 | 06 | Ready | Verification: 12+ chat→canvas E2E test scenarios |

## Key Files Modified

- `server.js` — tool schema, tool calling, streaming collection, fallback inference, max_tokens
- `src/prompts/shared.js` — extended CANVAS_ACTION_TYPES, updated META_DIRECTIVES
- `src/prompts/chat.js` — removed length caps, deleted buildChatActionSystemPrompt
- `src/prompts/index.js` — removed buildChatActionSystemPrompt export
- `public/app.js` — always stream, markdown rendering, reply event handling
- `public/app.html` — DOMPurify CDN, app.js already type=module
- `public/styles.css` — markdown styles, streaming cursor, copy button styles

## Blockers

None.

## Next Action

Resume with `/gsd-execute-phase 21` to execute Waves 3 and 4 (Plans 05 and 06).
