---
phase: 11-research-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/app.js
  - public/styles.css
  - public/index.html
autonomous: true
requirements:
  - RMOD-01
  - RMOD-02
must_haves:
  truths:
    - "Card bottom-right 'Analyze' button is replaced with a 'Research' button"
    - "Clicking Research button directly does nothing; hover reveals dropdown"
    - "Dropdown contains 'Analyze' and 'Explore' options"
    - "Hovering each option shows a tooltip bubble explaining the mode"
  artifacts:
    - path: "public/index.html"
      provides: "Research button markup with dropdown"
      contains: "research-button"
    - path: "public/styles.css"
      provides: "Dropdown and tooltip styling"
---

<objective>
Replace the single "Analyze" button with a "Research" hover-dropdown containing "Analyze" (no-thinking) and "Explore" (thinking) modes, each with explanatory tooltips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/index.html
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace analyze button with research dropdown markup</name>
  <files>public/index.html, public/app.js</files>
  <read_first>
    <file>public/index.html</file>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `public/index.html`, find the analyze button (`#analyzeButton`) and replace it with:
   ```html
   <div class="research-dropdown-wrapper">
     <button id="researchButton" class="research-button" type="button">研究</button>
     <div class="research-dropdown">
       <div class="research-option" data-mode="analyze">
         <span class="option-label">分析</span>
         <div class="option-tooltip">调用 no-thinking 模式，快速视觉分析</div>
       </div>
       <div class="research-option" data-mode="explore">
         <span class="option-label">探索</span>
         <div class="option-tooltip">调用 thinking 模式，深入分析并搜集相关资料</div>
       </div>
     </div>
   </div>
   ```
2. In `public/app.js`, update event handlers:
   - Remove direct `#analyzeButton` click handler
   - Add hover handlers for `.research-dropdown-wrapper` to show/hide dropdown
   - Add click handlers for `.research-option[data-mode="analyze"]` calling existing `analyzeSource()`
   - Add click handlers for `.research-option[data-mode="explore"]` calling new `exploreSource()`
3. Add i18n keys: `research.button`, `research.analyze`, `research.explore`, `research.analyzeTooltip`, `research.exploreTooltip`
  </action>
  <verify>
    <automated>grep -n "research-button\|research-dropdown" public/index.html public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Research button exists in HTML
    - Dropdown shows on hover
    - Analyze option triggers existing analysis flow
    - Explore option triggers new explore flow
  </acceptance_criteria>
  <done>Research dropdown UI implemented with i18n.</done>
</task>

<task type="auto">
  <name>Task 2: Style research dropdown and tooltips</name>
  <files>public/styles.css</files>
  <read_first>
    <file>public/styles.css</file>
  </read_first>
  <action>
1. Style `.research-dropdown-wrapper` as inline-block positioned relative
2. `.research-button`: styled like previous analyze button
3. `.research-dropdown`: absolute, hidden by default, shown on parent hover
4. `.research-option`: padding, hover highlight, cursor pointer
5. `.option-tooltip`: absolute, hidden by default, shown on option hover; small bubble with arrow, dark background, white text
6. Dark mode compatibility for all new elements
  </action>
  <verify>
    <automated>grep -n "research-\|option-tooltip" public/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - Dropdown appears below button on hover
    - Tooltips appear next to option on hover
    - All elements styled in both light and dark mode
  </acceptance_criteria>
  <done>Research dropdown and tooltips styled.</done>
</task>

<task type="auto">
  <name>Task 3: Wire research modes to handlers</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Rename/refactor existing `handleAnalyze()` to `handleAnalyze(mode)` where mode defaults to "analyze"
2. Create `handleExplore()` stub that will be implemented in 11-02
3. Ensure both handlers respect `state.selectedNodeId` (only source/analysis nodes can be researched)
4. Show toast if user tries to research a generated/option node: "该卡片无法进行研究"
5. Add i18n: `research.cannotResearch`
  </action>
  <verify>
    <automated>grep -n "handleExplore\|handleAnalyze" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Both analyze and explore handlers exist
    - Handlers check selected node type before proceeding
    - Toast shown for invalid node types
  </acceptance_criteria>
  <done>Research modes wired to handlers with validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | Research mode is user choice; both modes call same API endpoints |
</threat_model>

<verification>
- [ ] Research button shows dropdown on hover
- [ ] Analyze option triggers analysis
- [ ] Explore option triggers explore stub
- [ ] Tooltips show on option hover
- [ ] Invalid node types show toast
- [ ] i18n works for all labels
</verification>

<success_criteria>
- Analyze button replaced with Research dropdown
- Dropdown contains Analyze and Explore options with tooltips
- Both modes are wired to appropriate handlers
- UI works in both light and dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/11-research-mode/11-01-SUMMARY.md`
</output>
