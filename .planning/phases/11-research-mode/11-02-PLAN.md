---
phase: 11-research-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - public/app.js
  - server.js
  - src/api/settings.js
autonomous: true
requirements:
  - RMOD-03
must_haves:
  truths:
    - "Explore mode calls the analysis API with thinking mode enabled"
    - "API returns creative directions plus gathered reference materials"
    - "Reference materials are stored in node.data.references array"
    - "References include: title, url, description, type (web/doc/image)"
  artifacts:
    - path: "public/app.js"
      provides: "Explore mode handler and reference data storage"
      contains: "handleExplore"
    - path: "server.js"
      provides: "POST /api/analyze-explore endpoint"
---

<objective>
Implement the Explore mode backend and frontend: thinking-mode analysis that returns creative directions plus gathered reference materials stored in node data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@server.js
@src/api/settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explore endpoint to server</name>
  <files>server.js</files>
  <read_first>
    <file>server.js</file>
    <file>src/api/settings.js</file>
  </read_first>
  <action>
1. In `server.js`, add `POST /api/analyze-explore` endpoint:
   - Reuses existing analysis logic but with thinking mode enabled
   - Prompt instructs model to also search for and summarize relevant reference materials (websites, documents, similar images)
   - Returns JSON with: `title`, `summary`, `detectedSubjects`, `moodKeywords`, `options` (array), `references` (array)
   - Each reference: `{ title, url, description, type: "web"|"doc"|"image" }`
2. Use `ANALYSIS_CONFIG` with thinking mode parameter (reuse 10-03 thinking mode wiring)
3. Demo mode returns demo explore data with sample references
  </action>
  <verify>
    <automated>grep -n "/api/analyze-explore" server.js</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/analyze-explore endpoint exists
    - Returns options + references array
    - Demo mode works without API key
  </acceptance_criteria>
  <done>Explore API endpoint implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explore handler in canvas app</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Implement `handleExplore()`:
   - Gets selected source/analysis node
   - Sends image/text to `/api/analyze-explore` with `thinkingMode: "thinking"`
   - Receives response with options and references
   - Creates analysis node (same as analyze flow)
   - Creates option nodes, each with `data.references` from the response
   - Renders all nodes
2. Reference data structure stored in option node:
   ```js
   node.data.references = [
     { title: "...", url: "...", description: "...", type: "web" }
   ]
   ```
3. Add visual indicator on option nodes that have references (small badge/icon)
4. Add i18n: `research.exploring`, `research.exploreComplete`
  </action>
  <verify>
    <automated>grep -n "handleExplore\|references" public/app.js | head -20</automated>
  </verify>
  <acceptance_criteria>
    - handleExplore() calls /api/analyze-explore
    - Option nodes store references in data.references
    - Visual indicator shows on nodes with references
  </acceptance_criteria>
  <done>Explore handler implemented with reference storage.</done>
</task>

<task type="auto">
  <name>Task 3: Update session serialization for references</name>
  <files>src/api/sessions.js</files>
  <read_first>
    <file>src/api/sessions.js</file>
  </read_first>
  <action>
1. In `serializeState()`, ensure `node.data.references` is included in source node data
2. In deserialize/restore logic, ensure `references` array is restored on option nodes
3. Update Prisma schema if needed (or ensure JSON field stores references)
  </action>
  <verify>
    <automated>grep -n "references" src/api/sessions.js</automated>
  </verify>
  <acceptance_criteria>
    - References survive session save/load
    - No data loss for explore-generated nodes
  </acceptance_criteria>
  <done>Session serialization handles reference data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | References are user-visible metadata; URLs should be validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Reference URLs in node data | accept | URLs are user-facing by design |
| T-11-02 | Tampering | Fake reference data from model | accept | Model-generated references are suggestions only |
</threat_model>

<verification>
- [ ] POST /api/analyze-explore returns options + references
- [ ] Explore handler creates option nodes with references
- [ ] Reference badge shows on explore-generated nodes
- [ ] References persist after save/load
- [ ] Demo mode returns sample references
</verification>

<success_criteria>
- Explore mode calls thinking-enabled analysis API
- Response includes creative directions + reference materials
- Reference materials stored in option node data
- Visual indicator shows which nodes have references
- Session persistence handles references correctly
</success_criteria>

<output>
After completion, create `.planning/phases/11-research-mode/11-02-SUMMARY.md`
</output>
