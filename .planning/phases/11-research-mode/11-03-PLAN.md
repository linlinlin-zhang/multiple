---
phase: 11-research-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - public/app.js
  - public/styles.css
  - public/index.html
autonomous: true
requirements:
  - RMOD-04
must_haves:
  truths:
    - "Double-clicking an option node with references opens a reference modal"
    - "Modal shows list of references with title, URL, description"
    - "References are clickable links opening in new tab"
    - "Modal can be closed via X, backdrop click, or Escape"
  artifacts:
    - path: "public/index.html"
      provides: "Reference modal markup"
      contains: "id=\"referenceModal\""
    - path: "public/app.js"
      provides: "openReferenceModal() and closeReferenceModal()"
---

<objective>
Implement a reference materials modal that opens when double-clicking an option node that contains explore-generated references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/index.html
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference modal markup</name>
  <files>public/index.html</files>
  <read_first>
    <file>public/index.html</file>
  </read_first>
  <action>
1. Add reference modal HTML after the image viewer modal:
   ```html
   <div id="referenceModal" class="modal reference-modal" role="dialog" aria-modal="true" style="display:none;">
     <div class="modal-backdrop"></div>
     <div class="modal-content">
       <button class="modal-close" aria-label="Close">&times;</button>
       <h3 class="reference-modal-title">参考资料</h3>
       <div class="reference-list"></div>
     </div>
   </div>
   ```
2. Ensure modal is hidden by default
  </action>
  <verify>
    <automated>grep -n "referenceModal" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Reference modal markup exists in HTML
    - Hidden by default
  </acceptance_criteria>
  <done>Reference modal markup added.</done>
</task>

<task type="auto">
  <name>Task 2: Style reference modal</name>
  <files>public/styles.css</files>
  <read_first>
    <file>public/styles.css</file>
  </read_first>
  <action>
1. Reuse existing modal styles where possible
2. `.reference-modal .modal-content`: max-width 640px, max-height 80vh
3. `.reference-list`: scrollable, padding
4. `.reference-item`: card-like row with title, URL, description
5. `.reference-item a`: blue link, underline on hover
6. `.reference-item .ref-type-badge`: small badge indicating type (web/doc/image)
7. Dark mode compatibility
  </action>
  <verify>
    <automated>grep -n "reference-modal\|reference-list\|reference-item" public/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - Modal styled consistently with image viewer modal
    - Reference items are readable with clear type badges
    - Scrollable for long reference lists
  </acceptance_criteria>
  <done>Reference modal styled.</done>
</task>

<task type="auto">
  <name>Task 3: Implement modal logic and wiring</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Implement `openReferenceModal(nodeId)`:
   - Find node by ID
   - Get `node.data.references` array
   - Populate `.reference-list` with items:
     ```html
     <div class="reference-item">
       <span class="ref-type-badge">{type}</span>
       <a href="{url}" target="_blank" rel="noopener">{title}</a>
       <p>{description}</p>
     </div>
     ```
   - Show modal
2. Implement `closeReferenceModal()`:
   - Hide modal
   - Clear reference list
3. Wire double-click on option nodes:
   - In node registration, add dblclick handler that checks if node has references
   - If yes, call `openReferenceModal(node.id)` instead of/selectively with selection toggle
   - Ensure this doesn't conflict with existing selection double-click
   - Approach: triple-click or alt+double-click for references, OR check if node has references and prioritize reference modal
4. Handle Escape key and backdrop click for closing
5. Add i18n: `reference.title`, `reference.empty`
  </action>
  <verify>
    <automated>grep -n "openReferenceModal\|referenceModal" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Double-clicking option node with references opens modal
    - Modal shows all references with clickable links
    - Modal closes via X, backdrop, or Escape
    - Nodes without references behave normally (selection toggle)
  </acceptance_criteria>
  <done>Reference modal logic and wiring complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> external | Reference URLs open in new tab; standard browser protections apply |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | Reference URLs may be malicious | accept | URLs open with noopener; user discretion applies |
</threat_model>

<verification>
- [ ] Double-click option with references opens modal
- [ ] Modal displays title, URL, description for each reference
- [ ] Links open in new tab with noopener
- [ ] Modal closes via X, backdrop, Escape
- [ ] Nodes without references still toggle selection on double-click
- [ ] i18n works for modal title
</verification>

<success_criteria>
- Reference modal opens when double-clicking nodes with explore references
- Modal displays all reference materials with clickable links
- UI is consistent with existing modals
- Selection behavior preserved for nodes without references
</success_criteria>

<output>
After completion, create `.planning/phases/11-research-mode/11-03-SUMMARY.md`
</output>
