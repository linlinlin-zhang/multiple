---
phase: 10-dialog-refactor-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - public/app.js
  - public/styles.css
  - public/index.html
autonomous: true
requirements:
  - DGEN-03
must_haves:
  truths:
    - "A thinking mode toggle exists to the left of the send button"
    - "Default display is transparent '思考 ∨', hover reveals semi-transparent dropdown"
    - "Options are '思考' (thinking) and '快速' (no-thinking)"
    - "Selected mode affects the model parameter for the current request"
    - "Preference is persisted in localStorage"
  artifacts:
    - path: "public/index.html"
      provides: "Thinking mode toggle markup"
      contains: "thinking-toggle"
    - path: "public/app.js"
      provides: "Thinking mode state management and API parameter override"
      contains: "thinkingMode"
    - path: "public/styles.css"
      provides: "Toggle styling with transparent/hover states"
---

<objective>
Add a thinking mode toggle next to the chat send button. Users can switch between thinking (deep reasoning) and no-thinking (fast response) modes, affecting how the AI model processes their requests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/index.html
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thinking mode toggle markup and styles</name>
  <files>public/index.html, public/styles.css</files>
  <read_first>
    <file>public/index.html</file>
    <file>public/styles.css</file>
  </read_first>
  <action>
1. In `public/index.html`, add a thinking mode toggle to the left of the send button in the chat bar:
   ```html
   <div class="thinking-toggle-wrapper">
     <button id="thinkingToggle" class="thinking-toggle" type="button">
       <span class="thinking-label">思考</span>
       <span class="thinking-arrow">∨</span>
     </button>
     <div class="thinking-dropdown">
       <button class="thinking-option" data-mode="thinking">思考</button>
       <button class="thinking-option" data-mode="no-thinking">快速</button>
     </div>
   </div>
   ```
2. In `public/styles.css`:
   - `.thinking-toggle`: transparent background, small text, subtle border
   - `.thinking-toggle:hover`: semi-transparent background, slight shadow
   - `.thinking-dropdown`: hidden by default, shown on hover/focus
   - `.thinking-option`: styled buttons for each mode
   - Active mode has subtle highlight
   - Dark mode compatibility
3. Add i18n keys: `thinking.mode`, `thinking.thinking`, `thinking.fast`
  </action>
  <verify>
    <automated>grep -n "thinking-toggle\|thinking-dropdown" public/index.html public/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - Toggle markup exists in chat bar
    - CSS provides transparent default and hover reveal
    - Dropdown contains thinking and fast options
    - i18n keys added to dictionary
  </acceptance_criteria>
  <done>Thinking mode toggle UI implemented with styling and i18n.</done>
</task>

<task type="auto">
  <name>Task 2: Implement thinking mode state and persistence</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Add `state.thinkingMode = "no-thinking"` (default to fast mode).
2. Implement `setThinkingMode(mode)`:
   - Validates mode is "thinking" or "no-thinking"
   - Updates `state.thinkingMode`
   - Updates toggle label text
   - Saves to `localStorage.setItem("oryzae-thinking-mode", mode)`
3. Implement `loadThinkingMode()`:
   - Reads from localStorage on init
   - Defaults to "no-thinking" if not set
4. Wire toggle button click handlers:
   - Click option -> `setThinkingMode(option.dataset.mode)`
   - Close dropdown after selection
5. Call `loadThinkingMode()` in `init()`.
  </action>
  <verify>
    <automated>grep -n "thinkingMode\|setThinkingMode\|loadThinkingMode" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `state.thinkingMode` exists and defaults to "no-thinking"
    - `setThinkingMode()` updates state, UI, and localStorage
    - `loadThinkingMode()` restores preference on init
    - Toggle click handlers wired correctly
  </acceptance_criteria>
  <done>Thinking mode state management and persistence implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Wire thinking mode to API requests</name>
  <files>public/app.js, src/api/settings.js, server.js</files>
  <read_first>
    <file>public/app.js</file>
    <file>server.js</file>
  </read_first>
  <action>
1. In `public/app.js`, when making chat or generation API calls, include `thinkingMode` in the request body:
   ```js
   body: JSON.stringify({ messages, thinkingMode: state.thinkingMode })
   ```
2. In `server.js`, update chat and analysis endpoints to accept `thinkingMode`:
   - If `thinkingMode === "thinking"`, use a model variant that supports thinking (or add a parameter)
   - If `thinkingMode === "no-thinking"`, use standard no-thinking model
   - For now, this can be a pass-through to the model API (many APIs support a `thinking` or `reasoning` parameter)
3. Alternatively, if the API doesn't support thinking mode directly, document that the parameter is passed and the model configuration in settings determines the behavior.
4. Ensure the thinking mode only affects the *current* request (not persisted to settings).
  </action>
  <verify>
    <automated>grep -n "thinkingMode" public/app.js server.js</automated>
  </verify>
  <acceptance_criteria>
    - Chat/generation requests include `thinkingMode` parameter
    - Server endpoints accept and handle `thinkingMode`
    - Mode affects the current request only
  </acceptance_criteria>
  <done>Thinking mode wired to API requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | thinkingMode is user-controlled cosmetic/behavioral parameter |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | localStorage "oryzae-thinking-mode" | accept | Value is cosmetic only; invalid values fall back to default |
| T-10-04 | Information Disclosure | Thinking mode preference in localStorage | accept | Not sensitive data |
</threat_model>

<verification>
- [ ] Toggle UI renders correctly with transparent default and hover reveal
- [ ] Clicking option updates label and saves to localStorage
- [ ] Reloading page restores last selected mode
- [ ] Chat/generation requests include thinkingMode parameter
- [ ] i18n works for toggle labels in both languages
- [ ] Dark mode compatible
</verification>

<success_criteria>
- Thinking mode toggle exists left of send button with transparent/hover UX
- Mode selection persists across sessions via localStorage
- Selected mode is sent with API requests and affects model behavior
- All UI text is i18n-ready
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-refactor-generation/10-03-SUMMARY.md`
</output>
