---
phase: 10-dialog-refactor-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/app.js
  - public/styles.css
autonomous: true
requirements:
  - DGEN-01
must_haves:
  truths:
    - "Chat messages are sent with the selected card's context as the anchor"
    - "System prompt includes selected card title, type, and content summary"
    - "Chat history is scoped to the selected card's branch"
    - "Changing selection updates the dialog placeholder to reflect the selected card"
  artifacts:
    - path: "public/app.js"
      provides: "Chat handler refactored to use selectedNodeId as context anchor"
      contains: "selectedNodeId"
    - path: "public/styles.css"
      provides: "Dialog state visual feedback for selected card context"
---

<objective>
Refactor the chat/dialog system to bind all conversation context to the currently selected card. The chat assistant always understands which card the user is talking about.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor handleChat to use selected card as context anchor</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `handleChat()`, get the currently selected node via `state.selectedNodeId`.
2. Build a context summary from the selected node:
   - Type: source/analysis/option/generated
   - Title: node.data.title or node.data.option.title
   - Summary: node.data.summary or node.data.explanation
   - Prompt: node.data.prompt (for option/generated)
3. Include this context in the system prompt sent to the chat model:
   ```
   当前用户正在与画布上的以下卡片对话：
   类型：{type}
   标题：{title}
   内容摘要：{summary}
   ```
4. Ensure chat messages are appended to `state.chatMessages` as before, but the assistant's responses are contextually aware of the selected card.
  </action>
  <verify>
    <automated>grep -n "selectedNodeId" public/app.js | grep -i "chat\|handleChat"</automated>
  </verify>
  <acceptance_criteria>
    - `handleChat()` references `state.selectedNodeId` to build context
    - System prompt includes selected card type and title
  </acceptance_criteria>
  <done>Chat handler uses selected card as conversational anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Update dialog placeholder based on selected card</name>
  <files>public/app.js, public/styles.css</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `updateDialogState()` (from 09-02), when a card is selected, update the chat input placeholder to include the selected card's title (truncated):
   ```js
   chatInput.placeholder = t("chat.placeholderWithCard", { title: truncatedTitle });
   ```
2. Add i18n keys:
   - zh: `chat.placeholderWithCard`: "与 '{title}' 对话..."
   - en: `chat.placeholderWithCard`: "Chat with '{title}'..."
3. Style the focused chat input to subtly indicate the bound card context (optional border color change).
  </action>
  <verify>
    <automated>grep -n "placeholderWithCard" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `updateDialogState()` updates placeholder with selected card title
    - i18n dictionary contains `chat.placeholderWithCard` for zh and en
  </acceptance_criteria>
  <done>Dialog placeholder reflects the selected card context.</done>
</task>

<task type="auto">
  <name>Task 3: Scope chat history display to selected branch</name>
  <files>public/app.js, public/styles.css</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Add `branchNodeId` field to chat messages (optional: to track which branch a message belongs to).
2. When rendering chat history, optionally filter or group messages by the selected card's branch.
3. For now, ensure the chat UI clearly shows which card is active (add a small indicator above the chat input showing the selected card name).
  </action>
  <verify>
    <automated>grep -n "branchNodeId\|selected.*chat" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Chat UI shows an indicator of the currently selected card
    - Messages are contextually scoped to the selected card
  </acceptance_criteria>
  <done>Chat history is visually scoped to the selected card branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | Chat context includes selected card data; no sensitive exposure |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | Selected card title in placeholder | accept | Title is user-visible UI text |
</threat_model>

<verification>
- [ ] Chat sends selected card context in system prompt
- [ ] Placeholder updates when selection changes
- [ ] Chat indicator shows selected card name
- [ ] i18n works for placeholder text
</verification>

<success_criteria>
- All chat messages are contextually anchored to the selected card
- Dialog UI clearly indicates which card is active
- System prompt includes selected card type, title, and summary
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-refactor-generation/10-01-SUMMARY.md`
</output>
