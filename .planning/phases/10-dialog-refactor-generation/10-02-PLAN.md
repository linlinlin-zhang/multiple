---
phase: 10-dialog-refactor-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - public/app.js
  - public/styles.css
  - public/index.html
autonomous: true
requirements:
  - DGEN-02
must_haves:
  truths:
    - "User can select a card and type a direction/constraint in the dialog to generate a new option node"
    - "The new option node is created as a child of the selected card"
    - "The option node uses the dialog text as its prompt and title"
    - "The user can then click the new option to generate an image"
  artifacts:
    - path: "public/app.js"
      provides: "Direct generation from dialog input"
      contains: "generateDirectionFromDialog"
    - path: "public/index.html"
      provides: "Visual indicator for direct generation mode"
---

<objective>
Allow users to generate new creative directions directly from the chat dialog without using the research button. Selected card + dialog input = new option node.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/index.html
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement direct generation command in dialog</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `handleChat()`, detect if the user input starts with a generation command prefix (e.g., `/gen `, `/生成 `) or use a special UI mode.
2. Alternatively, add a small "生成" (Generate) button next to the send button that triggers direct generation mode.
3. When triggered:
   - Get selected node
   - Create a new option node as child of selected node
   - Use the dialog text as the option's `title`, `description`, and `prompt`
   - Position the new node near the selected node (offset to the right/below)
   - Add link from selected node to new option node
   - Render the new option node
4. Clear the dialog input after generation.
  </action>
  <verify>
    <automated>grep -n "generateDirectionFromDialog\|createOptionNode\|new option" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Dialog can trigger creation of new option nodes
    - New option node is child of selected card
    - Option data includes user-provided title/description/prompt
  </acceptance_criteria>
  <done>Direct generation from dialog is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add generate button to chat bar</name>
  <files>public/index.html, public/styles.css, public/app.js</files>
  <read_first>
    <file>public/index.html</file>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `public/index.html`, add a small "生成" (Generate) button next to `#chatSendButton` in the chat bar:
   ```html
   <button id="chatGenerateButton" class="chat-generate-button" type="button" title="生成方向">生成</button>
   ```
2. Style it as a small secondary button (smaller than send, subtle background).
3. In `public/app.js`, wire the button:
   - Disabled when no card is selected
   - On click: use current input text to create option node
   - If input is empty, show toast "请输入方向描述"
4. Add i18n keys: `chat.generate`, `chat.emptyPrompt`
  </action>
  <verify>
    <automated>grep -n "chatGenerateButton\|chat.generate" public/app.js public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Generate button exists in chat bar HTML
    - Button is wired in app.js
    - Button disabled state matches selection state
  </acceptance_criteria>
  <done>Generate button added to chat bar with full wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Handle edge cases for direct generation</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. Prevent generation if selected card is a "generated" node (only source/analysis/option can have children).
2. Show toast if user tries to generate from a generated node: "生成图节点无法继续生成方向".
3. Auto-select the newly created option node after generation.
4. Save session state after creating the new node.
  </action>
  <verify>
    <automated>grep -n "generated.*cannot\|cannot.*generate\|autoSelect" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Generated nodes cannot spawn new options
    - New option is auto-selected after creation
    - Session auto-saves after node creation
  </acceptance_criteria>
  <done>Edge cases handled for direct generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | User-provided prompt sent to image generation API; standard input validation applies |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | User-provided prompt for generation | accept | Prompt is user-controlled by design; no security boundary crossed |
</threat_model>

<verification>
- [ ] Select a card, type in dialog, click generate -> new option node appears as child
- [ ] New option has correct title/description/prompt from dialog text
- [ ] Generate button disabled when no card selected
- [ ] Generated nodes cannot spawn new options
- [ ] i18n works for all new UI text
</verification>

<success_criteria>
- User can generate new option nodes directly from dialog without research button
- New option is always child of selected card
- Generate button is intuitive and properly disabled/enabled
- Edge cases (generated nodes, empty input) are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-refactor-generation/10-02-SUMMARY.md`
</output>
