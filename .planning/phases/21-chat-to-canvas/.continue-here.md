---
context: phase
phase: 21-chat-to-canvas
task: 6
total_tasks: 6
status: paused
last_updated: 2026-05-04T00:00:00.000Z
---

> **2026-05-04 update (this session):** While preparing for Plan 06 E2E, the user hit a chat 400 error: `"Agent mode does not support tools. You need to either avoid using enable_code_interpreter or avoid using the agent mode with enable_search."` Deep analysis confirmed that the `search_strategy: agent_max` adopted in commit 807c2db puts DashScope into **Agent mode**, which is mutually exclusive with the custom `tools: CANVAS_TOOLS` array. The proper fix (DashScope Responses API migration) is now scoped as v3.2 phase 26; for now we applied a one-line **path-B fallback** at `server.js:774` to unblock 21-06: `search_strategy: options.strategy || "agent_max"` → `search_strategy: options.strategy || "max"`. The `max` tier keeps multi-round web search but does not engage agent orchestration, so CANVAS_TOOLS continues to work. A new v3.2 milestone (Qwen Web Parity, 7 phases 26-32) was agreed with the user. **Working tree is dirty**: `server.js` (path-B fix) + `.planning/STATE.md` (pre-existing change). Resume by committing the fix first.

> **2026-05-04 earlier update:** 21-05-SUMMARY.md retroactively written and committed (db447f2). ROADMAP advanced to 5/6.

<current_state>
Phase 21 plans 01-05 are shipped. Plan 06 (E2E verification, 12+ scenarios) is the only remaining plan. The chat 400 blocker discovered this session has been mitigated with a one-line server.js change (path B). The proper Responses API migration is scoped as v3.2 phase 26. v3.2 milestone with 7 phases (26 Responses API / 27 Document Chat / 28 Image Polish / 29 Video / 30 Voice / 31 Deep Research Viz / 32 Embedding Upgrade) is agreed. Resume order: commit fix → run 21-06 E2E → ship phase 21 → /gsd-new-milestone v3.2.
</current_state>

<completed_work>

Plans (Waves 1-3):
- Plan 01-04: Done (commit 0f85b18)
- Plan 05 (Fallback & Action Feedback + Qwen-parity sub-track): Done (commits 807e529 → 807c2db, summary db447f2)

Parity-with-Qwen sub-track (prior session):
- 064f7a9 — fix chat tool-call regressions (empty reply, generic action types)
- e066d87 — adopt minimal/natural prompt style, raise max_tokens to 32768
- 37759db — document content shapes in canvas_action tool schema + encourage multi-call per turn
- b23d12f — widen web-search heuristic to travel/lifestyle queries
- 0323a67 — render GFM tables / task lists / strikethrough in chat replies
- be10a88 — render plan/todo/weather/map/link/code content inside option cards
- 5d336e5 — loosen input/output caps to fit Qwen3.6 Plus 991K/64K window
- 4f60c16 — widen front-end text-paste cap to 32K
- 807c2db — adopt agent_max + forced_search + enable_citation + default-on heuristic

This session (analysis + path-B fix, NOT yet committed):
- Identified that `agent_max` (commit 807c2db) is Qwen-web's private internal tier triggering Agent mode → mutually exclusive with `tools: CANVAS_TOOLS`. This is what produced the 400 error.
- Mapped the full DashScope API surface (10 capability groups): Chat Completions / Responses / Application / Files+qwen-long / Image (sync+async+edit) / Video (wan2.x) / Voice (omni-plus-realtime + ASR + TTS) / Translation / Embedding+rerank / Deep Research streaming.
- Applied path-B fix at server.js:774: `"agent_max"` → `"max"`. Verified via grep that no other code path references `agent_max` (only historical `.planning/` text).
- Designed v3.2 milestone (Qwen Web Parity) with 7 sequential phases — user approved.
</completed_work>

<remaining_work>

Phase 21 (immediate):
- Commit the path-B fix (suggested message: `fix(21): fall back search_strategy to max to unblock CANVAS_TOOLS coexistence`). Either standalone or bundled with 21-06 commit.
- Start `npm run dev` in a second terminal.
- Plan 06 — E2E verification script (12+ scenarios) — autonomous: false.
- 21-06-SUMMARY.md.
- Phase 21 verification (gsd-verifier subagent + gates).
- STATE.md + ROADMAP.md updates (mark Phase 21 ✅, advance v3.1 to 1/6).
- Production smoke test: confirm `max` search strategy is accepted by qwen3.6-plus. If rejected, fall back to plain `enable_search: true` without `search_strategy` (pre-807c2db behavior).

After Phase 21 ships (v3.2 kickoff):
- `/gsd-new-milestone v3.2 "Qwen Web Parity"`
- `/gsd-phase add 26 "Responses API Migration"`
- `/gsd-plan-phase 26` — break down into 6 plans:
  - 26-01: Responses API client adapter (DashScope endpoint `/api/v2/apps/protocols/compatible-mode/v1/responses`)
  - 26-02: Tool merge — builtin `web_search` + `code_interpreter` + `web_extractor` alongside CANVAS_TOOLS function calls
  - 26-03: Output parser — handle Responses API's `output[]` array (text / tool_call / web_search_result / code_interpreter_result blocks)
  - 26-04: `previous_response_id` plumbing for multi-turn (replaces messages[] in chat continuations)
  - 26-05: Reference rendering — render `[ref_<n>]` citations as clickable inline links pointing to web_search_result panel
  - 26-06: Artifact viewer for code_interpreter outputs (charts, tables, generated files)
- Then sequentially: phase 27 (Document Chat — Files API + qwen-long), 28 (Image Polish — async task_id polling, qwen-image-max+plus, edit), 29 (Video — wan2.x text-to-video / image-to-video / wan2.2-s2v digital human), 30 (Voice — qwen3.5-omni-plus-realtime websocket + qwen3-asr-flash-realtime + qwen3-tts-flash), 31 (Deep Research Viz — streamingWebResult/WebResultFinished/KeepAlive phase UI), 32 (Embedding Upgrade — text-embedding-v4 with instruct + gte-rerank-v2 for material library RAG).
</remaining_work>

<decisions_made>

This session:
- Path B (one-line fix) over path A (immediate Responses API migration) — minimum diff to unblock 21-06; proper migration scoped as v3.2 phase 26
- New v3.2 milestone over extending v3.1 — Qwen-web-parity is materially new scope (Responses API + document chat + image/video/voice + RAG)
- Sequential phase execution (26 → 32) over parallel tracks — user preference: conservative; phase 26 is keystone (unlocks builtin tools without losing CANVAS_TOOLS)
- `max` over `agent` for path-B fallback — `agent` ALSO forbids tools; `max` keeps multi-round search without agent orchestration
- Keep `forced_search: true` + `enable_citation: true` + `enable_source: true` — those are non-agent options and do not interact with the tools array

Prior session (parity sub-track) — see `21-05-SUMMARY.md`:
- search_strategy: turbo → agent_max (this session reverted to `max`)
- Default web search ON with negation block-list
- enable_citation: true with citation_format [ref_<n>]
- Chat default max_tokens 32768 → 65536
- History depth 8 → 40 turns, per-msg cap 1200 → 8000 chars
- .option-rich-content slot + renderRichNodeContent helper
- micromark-extension-gfm for chat reply rendering
- Document content shapes per nodeType in canvas_action tool schema
</decisions_made>

<blockers>
- B1: Plan 06 marked autonomous: false — needs `npm run dev` running concurrently (human action)
- B2: `max` strategy needs production smoke test against current qwen3.6-plus snapshot. If rejected, fall back to plain `enable_search: true` without `search_strategy`. Phase 26 (Responses API) will eliminate this fragility entirely.
- B3 (long-standing): PostgreSQL not running locally (not phase-specific)
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state
2. `.planning/phases/21-chat-to-canvas/21-05-SUMMARY.md` — completed Plan 05 + Qwen-parity sub-track
3. `.planning/phases/21-chat-to-canvas/21-06-PLAN.md` — Plan 06 (next to execute)
4. `git diff server.js` — review the path-B one-line fix before committing
5. `server.js:765-778` (applyWebSearchMode) — the function the fix touches
6. DashScope Responses API docs (for v3.2 phase 26 prep): https://help.aliyun.com/zh/model-studio/responses-api

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: Adopting an undocumented private API tier from a screenshot/network capture. `agent_max` was Qwen-web internal — not in DashScope public docs. → STRUCTURAL: cite the official docs page when configuring SDK params; if the value isn't documented, treat it as a research/spike item rather than production code.
- ANTI-PATTERN: Adding `tools: [...]` and `enable_search: true` (with agent_*) without checking their interaction. They are mutually exclusive in DashScope Chat Completions; only the Responses API allows them to coexist. → STRUCTURAL: when combining LLM features (tools / search / code interpreter / artifacts), check the API's compatibility matrix before wiring; for DashScope, that lives at the Responses API page.
- ANTI-PATTERN (still relevant from prior session): Adding caps without citing model spec. → STRUCTURAL: cite Qwen3.6 Plus 991K/64K when changing token caps.
- ANTI-PATTERN (still relevant): Designing heuristics as allow-lists. → STRUCTURAL: prefer default-on with negation block-list.
- ANTI-PATTERN (still relevant): Telling LLM about a tool without parameter shape. → STRUCTURAL: every tool param must have its concrete shape (with example) in the description.

## Infrastructure State

- Dev server: not running (must start `npm run dev` for Plan 06)
- PostgreSQL: not running locally (long-standing blocker)
- Working tree: **dirty** — `server.js` (path-B fix at line 774) + `.planning/STATE.md` (pre-existing change unrelated to this fix)
- Last commit on branch: 81abe13 (`docs(21): update .continue-here.md after SUMMARY backfill`)

<context>
Mental model on resume: phase 21 was fundamentally complete — only E2E verification + summary remained — but the user attempted a real run, hit the 400, and the issue turned out to expose a deeper limitation in the DashScope Chat Completions endpoint. Rather than deep-refactor mid-phase, we agreed: (1) ship 21 with the minimal fix, (2) open v3.2 explicitly for Qwen-web parity. The v3.2 plan covers everything needed to make this demo feel like the official Qwen application: builtin web search + code interpreter alongside our custom canvas tools (phase 26 keystone), document chat with Files API + qwen-long for 10M context (phase 27), full image/video/voice generation (phases 28-30), deep research streaming UI (phase 31), and modern embedding for material RAG (phase 32). DO NOT re-litigate the v3.2 scope on resume — it's agreed. Just execute.

Key reference parameters captured this session for v3.2 planning:
- Responses API endpoint: `/api/v2/apps/protocols/compatible-mode/v1/responses`
- Responses API allows: `tools: [{type: "web_search"}, {type: "code_interpreter"}, {type: "web_extractor"}, {type: "function", function: {...}}]` — all coexist
- Files API: `/v1/files` with `purpose=file-extract` returns `file-id` → reference via system message `fileid://<id>` against qwen-long (10M ctx)
- Image: qwen-image-2.0-pro (sync), qwen-image-max (async task_id, 24h validity), qwen-image-plus (edit/inpaint)
- Video: wan2.2-text-to-video, wan2.2-image-to-video, wan2.2-first-and-last-frame, wan2.2-s2v (digital human, audio-driven), all async 1-5min
- Voice realtime: qwen3.5-omni-plus-realtime (websocket, header `OpenAI-Beta: realtime=v1`, 120min session, server_vad)
- ASR: qwen3-asr-flash-realtime (auto language), paraformer-realtime-v2 (with language_hints)
- TTS: qwen3-tts-flash, cosyvoice-v3.5-plus (9 voices: Cherry/Dylan/Chelsie/Serena/Ethan, etc.), realtime variants
- Translation: qwen-mt-turbo / qwen-mt-plus with translation_options{source_lang, target_lang, terms[], domains, tm_list}
- Embedding: text-embedding-v4 (Qwen3-Embedding, 64-2048 configurable dims, instruct param), gte-rerank-v2
- Deep research: qwen-deep-research streams phases streamingWebResult / WebResultFinished / KeepAlive / finished
</context>

<next_action>
1. `git diff server.js` — review the one-line path-B fix at line 774.
2. `git add server.js && git commit -m "fix(21): fall back search_strategy to max to unblock CANVAS_TOOLS coexistence"` — commit the fix.
3. Start `npm run dev` in second terminal.
4. Execute Plan 06 — E2E verification script (see `21-06-PLAN.md`).
5. Production smoke test: send "三天深圳行程" → confirm reply has citations + plan card has step list. If `max` rejected, fall back to plain `enable_search: true` without `search_strategy`.
6. Write `21-06-SUMMARY.md`.
7. Phase 21 verification + STATE.md + ROADMAP.md update (mark Phase 21 ✅, v3.1 1/6).
8. `/gsd-new-milestone v3.2 "Qwen Web Parity"`
9. `/gsd-phase add 26 "Responses API Migration"` then `/gsd-plan-phase 26`.
</next_action>
