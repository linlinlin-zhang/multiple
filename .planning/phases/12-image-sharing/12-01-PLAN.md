---
phase: 12-image-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/app.js
  - public/styles.css
  - public/index.html
autonomous: true
requirements:
  - ISH-01
must_haves:
  truths:
    - "Image viewer modal has a circular share button at the bottom"
    - "Clicking the share button triggers share flow"
    - "Button is styled as a circle with share icon"
  artifacts:
    - path: "public/index.html"
      provides: "Share button markup in image viewer modal"
      contains: "share-button"
    - path: "public/styles.css"
      provides: "Circular share button styling"
---

<objective>
Add a circular share button to the image viewer modal that triggers the image sharing flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@public/app.js
@public/index.html
@public/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add share button markup to image viewer modal</name>
  <files>public/index.html</files>
  <read_first>
    <file>public/index.html</file>
  </read_first>
  <action>
1. In the `#imageViewerModal`, add a circular share button below the image/explanation area:
   ```html
   <div class="image-viewer-actions">
     <!-- existing regenerate/modify/download buttons -->
     <button id="imageShareButton" class="share-button" type="button" title="分享">
       <svg><!-- share icon --></svg>
     </button>
   </div>
   ```
2. Use a simple share icon (SVG) or Unicode character (↗)
3. Add i18n key: `viewer.share`
  </action>
  <verify>
    <automated>grep -n "imageShareButton\|share-button" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Share button exists in image viewer modal
    - Button is circular
    - Has share icon
  </acceptance_criteria>
  <done>Share button markup added to image viewer modal.</done>
</task>

<task type="auto">
  <name>Task 2: Style share button</name>
  <files>public/styles.css</files>
  <read_first>
    <file>public/styles.css</file>
  </read_first>
  <action>
1. Style `.share-button`:
   - width: 44px, height: 44px
   - border-radius: 50% (circle)
   - background: var(--ps-blue) or semi-transparent
   - color: white
   - border: none
   - cursor: pointer
   - hover: scale up slightly, darker background
2. Position in `.image-viewer-actions` row alongside existing buttons
3. Dark mode compatibility
  </action>
  <verify>
    <automated>grep -n "share-button" public/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - Button is circular (44x44px)
    - Styled consistently with modal theme
    - Hover effect works
  </acceptance_criteria>
  <done>Share button styled.</done>
</task>

<task type="auto">
  <name>Task 3: Wire share button click handler</name>
  <files>public/app.js</files>
  <read_first>
    <file>public/app.js</file>
  </read_first>
  <action>
1. In `openImageViewer()`, wire `#imageShareButton` click:
   ```js
   const shareBtn = modal.querySelector("#imageShareButton");
   if (shareBtn) {
     shareBtn.onclick = () => handleShareImage(currentNodeId);
   }
   ```
2. Implement `handleShareImage(nodeId)` stub:
   - Gets the generated node data
   - Will be fully implemented in 12-02
   - For now, show a toast: "生成分享链接中..." / "Generating share link..."
3. Ensure button is re-wired each time modal opens
  </action>
  <verify>
    <automated>grep -n "handleShareImage\|imageShareButton" public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - Share button click handler wired
    - handleShareImage() stub exists
    - Toast shown on click
  </acceptance_criteria>
  <done>Share button wired to handler stub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> server | Share request includes image hash and node data; no sensitive data |
</threat_model>

<verification>
- [ ] Share button visible in image viewer modal
- [ ] Button is circular
- [ ] Clicking shows toast
- [ ] i18n works
</verification>

<success_criteria>
- Circular share button exists in image viewer modal
- Button is styled and positioned correctly
- Click triggers share handler stub
</success_criteria>

<output>
After completion, create `.planning/phases/12-image-sharing/12-01-SUMMARY.md`
</output>
