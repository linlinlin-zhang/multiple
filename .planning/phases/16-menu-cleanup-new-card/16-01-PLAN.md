---
phase: 16-menu-cleanup-new-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/app.js]
autonomous: true
requirements: [MC-13, MC-14, MC-15, MC-16]

must_haves:
  truths:
    - "Typing / in the chat input no longer shows zoom-in command"
    - "Typing / in the chat input no longer shows zoom-out command"
    - "Typing / in the chat input no longer shows history browser command"
    - "Typing / in the chat input no longer shows settings command"
    - "Remaining commands (save, export, import, sessions, fit, arrange) still appear in the menu"
  artifacts:
    - path: "public/app.js"
      provides: "Cleaned getWorkbenchCommands() with 6 commands instead of 10"
      contains: "getWorkbenchCommands"
  key_links:
    - from: "getWorkbenchCommands()"
      to: "getFilteredCommands()"
      via: "function call"
      pattern: "getWorkbenchCommands\\(\\)"
    - from: "executeWorkbenchCommand()"
      to: "removed command handlers"
      via: "dead code removal"
      pattern: "commandId === \"zoom-in\""
---

<objective>
Remove zoom-in, zoom-out, history browser, and settings commands from the `/` command menu.

Purpose: Streamline the `/` menu to show only canvas-relevant commands, removing navigation commands that are accessible via the sidebar nav.
Output: Updated `getWorkbenchCommands()` returning 6 commands, with dead handler code removed from `executeWorkbenchCommand()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove zoom-in, zoom-out, history, settings from getWorkbenchCommands and clean up handlers</name>
  <files>public/app.js</files>
  <read_first>
    - public/app.js (lines 801-962: getWorkbenchCommands, getFilteredCommands, executeWorkbenchCommand)
  </read_first>
  <action>
In `public/app.js`:

1. In `getWorkbenchCommands()` (line 801), remove these 4 entries from the returned array:
   - `{ id: "zoom-in", icon: "+", label: t("command.zoomIn"), description: t("command.zoomInDesc") }` (line 808)
   - `{ id: "zoom-out", icon: "-", label: t("command.zoomOut"), description: t("command.zoomOutDesc") }` (line 809)
   - `{ id: "history", icon: "H", label: t("command.history"), description: t("command.historyDesc") }` (line 811)
   - `{ id: "settings", icon: "C", label: t("command.settings"), description: t("command.settingsDesc") }` (line 812)

   The remaining array should contain exactly 6 entries: save, export, import, sessions, fit, arrange.

2. In `executeWorkbenchCommand()` (line 943), remove the dead handler branches for the removed commands:
   - Remove `if (commandId === "zoom-in") return zoomBy(0.08);` (line 952)
   - Remove `if (commandId === "zoom-out") return zoomBy(-0.08);` (line 953)
   - Remove the `if (commandId === "history") { ... }` block (lines 955-958)
   - Remove the `if (commandId === "settings") { ... }` block (lines 959-961)

3. Keep the translation keys (`command.zoomIn`, `command.zoomOut`, `command.history`, `command.settings` and their `*Desc` variants) in the i18n dictionaries. They may be used elsewhere (sidebar nav labels) and removing them is out of scope.
  </action>
  <verify>
    <automated>grep -c "zoom-in\|zoom-out\|\"history\"\|\"settings\"" public/app.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `getWorkbenchCommands()` returns an array with exactly 6 items (save, export, import, sessions, fit, arrange)
    - No `id: "zoom-in"` string appears in `getWorkbenchCommands()`
    - No `id: "zoom-out"` string appears in `getWorkbenchCommands()`
    - No `id: "history"` string appears in `getWorkbenchCommands()`
    - No `id: "settings"` string appears in `getWorkbenchCommands()`
    - `executeWorkbenchCommand()` has no handler for "zoom-in", "zoom-out", "history", or "settings"
    - `executeWorkbenchCommand()` still has handlers for "save", "export", "import", "sessions", "fit", "arrange"
    - `zoomBy` and `resetView` functions still exist (used by other code paths)
  </acceptance_criteria>
  <done>
    The `/` command menu shows exactly 6 commands: save, export, import, sessions, fit, arrange. The removed commands (zoom-in, zoom-out, history, settings) have no entries in the command array and no dead handler code in executeWorkbenchCommand. All remaining commands still function correctly.
  </done>
</task>

</tasks>

<verification>
After plan execution:
1. Open the app in a browser
2. Click the chat input and type `/`
3. Verify the command menu shows 6 items: save, export, import, sessions, fit, arrange
4. Verify zoom-in, zoom-out, history browser, and settings are NOT in the list
5. Click each remaining command to verify they still work
</verification>

<success_criteria>
- `getWorkbenchCommands()` returns exactly 6 commands
- No zoom/history/settings commands appear in the `/` menu
- All remaining commands (save, export, import, sessions, fit, arrange) function correctly
- No broken references or dead code for removed commands
</success_criteria>

<output>
After completion, create `.planning/phases/16-menu-cleanup-new-card/16-01-SUMMARY.md`
</output>
