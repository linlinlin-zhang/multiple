---
phase: 07-settings-personalization
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260426120000_add_settings/migration.sql
  - server.js
  - src/api/settings.js
  - public/app.js
  - public/index.html
  - public/styles.css
  - app/src/index.css
  - app/index.html
  - app/src/lib/i18n.ts
  - app/src/components/cabinet/FileCabinet.tsx
  - app/src/components/cabinet/HistoryPage.tsx
  - app/src/components/cabinet/Sidebar.tsx
  - app/src/components/cabinet/SidebarItem.tsx
  - app/src/components/cabinet/AssetDetailPane.tsx
  - app/src/components/cabinet/AssetSidebar.tsx
  - app/src/components/cabinet/ContentArea.tsx
  - app/src/components/cabinet/FolderTab.tsx
  - app/src/components/share/ShareViewerPage.tsx
autonomous: true
requirements:
  - SETT-03
user_setup: []
must_haves:
  truths:
    - "User can switch language between Chinese and English from the canvas app settings panel"
    - "Switching language immediately updates all visible text in the canvas app without reload"
    - "Switching language immediately updates all visible text in the history browser without reload"
    - "Language preference persists to the database and survives page refresh"
    - "All UI text appears in the selected language (Chinese or English)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Settings model extended with language field"
      contains: "language"
    - path: "public/app.js"
      provides: "Canvas app i18n dictionary and t() helper"
      contains: "function t("
    - path: "app/src/lib/i18n.ts"
      provides: "React app i18n dictionary and useI18n hook"
      exports: ["useI18n", "I18nProvider"]
    - path: "public/index.html"
      provides: "Canvas app inline language script"
      contains: "oryzae-lang"
    - path: "app/index.html"
      provides: "React app inline language script"
      contains: "oryzae-lang"
  key_links:
    - from: "public/app.js"
      to: "dictionary object"
      via: "t(key) lookup"
      pattern: "dict\[lang\]\[key\]"
    - from: "app/src/lib/i18n.ts"
      to: "dictionary object"
      via: "useI18n hook returns t()"
      pattern: "dict\[lang\]\[key\]"
    - from: "public/app.js"
      to: "/api/settings"
      via: "PUT language on toggle"
      pattern: "fetch.*language.*PUT"
    - from: "app/src/components/cabinet/FileCabinet.tsx"
      to: "useI18n"
      via: "import and usage"
      pattern: "useI18n"
---

<objective>
Implement a lightweight i18n system supporting Chinese (zh) and English (en) across both the vanilla JS canvas app and the React history browser, with instant language switching and database persistence.

Purpose: All UI text becomes translatable; users can switch language at any time and see immediate results across the entire application.
Output: Dictionary-based i18n modules for both frontend stacks, language toggle UI, and backend persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/app.js
@public/index.html
@public/styles.css
@app/src/components/cabinet/FileCabinet.tsx
@app/src/components/cabinet/HistoryPage.tsx
@app/src/components/cabinet/Sidebar.tsx
@app/src/components/cabinet/SidebarItem.tsx
@app/src/components/cabinet/AssetDetailPane.tsx
@app/src/components/cabinet/AssetSidebar.tsx
@app/src/components/cabinet/ContentArea.tsx
@app/src/components/cabinet/FolderTab.tsx
@app/src/components/share/ShareViewerPage.tsx

<interfaces>
From public/index.html (hardcoded Chinese text examples):
```html
<span class="nav-label">工作界面</span>
<span class="nav-label">历史浏览器</span>
<span class="nav-label">主页</span>
<span class="nav-label">历史会话</span>
<div class="nav-settings-label">设置</div>
<strong>上传图片或文档</strong>
<span>选择图片、Word、PDF、PPT 或 TXT，生成分支方向</span>
<button id="analyzeButton" class="primary-button" disabled>分析</button>
<input id="chatInput" class="chat-input" type="text" autocomplete="off" placeholder="输入想继续探索的方向、风格或约束" />
<button id="chatSendButton" class="chat-send-button" type="submit">发送</button>
```

From public/app.js (hardcoded Chinese text examples):
```js
"你是这个画布式图片生成应用里的创意对话助手。"
"你的任务是帮助用户理解当前图片、比较分支方向、提出新的生成建议，或把用户的想法整理成可执行的视觉方向。"
"回答用中文，保持简洁，通常 1-3 句。"
"你是一个视觉创意导演，正在为一个画布式图片生成应用分析用户上传的图片。"
"请快速理解图片内容、主体、氛围、可延展的叙事方向，并给出 5 个不同的成图方向。"
```

From app/src/components/cabinet/Sidebar.tsx:
```tsx
<span className="text-sm font-medium text-cabinet-ink">{title}</span>
```

From app/src/components/cabinet/HistoryPage.tsx:
```tsx
<span>历史会话</span>
```

From app/src/components/cabinet/AssetDetailPane.tsx (typical labels):
```tsx
// Labels like "生成图片", "来源", "上传时间", etc. are hardcoded in various components
```

From app/src/components/share/ShareViewerPage.tsx:
```tsx
// Contains hardcoded Chinese text for share page title and descriptions
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Settings schema with language field and backend support</name>
  <files>prisma/schema.prisma, prisma/migrations/20260426120000_add_settings/migration.sql, server.js, src/api/settings.js</files>
  <read_first>
    <file>prisma/schema.prisma</file>
    <file>src/api/settings.js</file>
    <file>server.js</file>
  </read_first>
  <action>
1. In `prisma/schema.prisma`, add `language String @default("zh")` to the `Settings` model. The model should now look like:
```prisma
model Settings {
  id        String   @id @default(uuid())
  role      String   @unique
  endpoint  String
  model     String
  apiKey    String
  temperature Float  @default(0.7)
  theme     String   @default("light")
  language  String   @default("zh")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```
2. Update the migration SQL to include the language column. Append to the existing migration file:
```sql
ALTER TABLE "Settings" ADD COLUMN "language" TEXT NOT NULL DEFAULT 'zh';
```
3. In `src/api/settings.js`, update `handleGetSettings`:
   - Query the global settings row (`role: "global"`) for `theme` and `language`.
   - Return `{ theme, language, analysis: {...}, chat: {...}, image: {...} }`.
   - Default language is `"zh"`.
4. Update `handleUpdateSettings` to accept `body.language` (string, either `"zh"` or `"en"`). If present, upsert the global row with the language value.
5. In `server.js`, ensure the GET and PUT route handlers pass through the new shape.
  </action>
  <verify>
    <automated>grep -n "language" prisma/schema.prisma && grep -n "language" src/api/settings.js && grep -n "language" server.js</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `language String @default("zh")` inside `model Settings`.
    - `prisma/migrations/20260426120000_add_settings/migration.sql` contains `ALTER TABLE "Settings" ADD COLUMN "language" TEXT NOT NULL DEFAULT 'zh';`.
    - `src/api/settings.js` returns an object with a top-level `language` property from the global row.
    - `src/api/settings.js` contains `prisma.settings.upsert` for `role: "global"` when handling `body.language`.
    - `server.js` contains `/api/settings` route handlers that call `handleGetSettings` and `handleUpdateSettings`.
  </acceptance_criteria>
  <done>Settings model has language field, migration SQL includes it, and settings API returns/accepts top-level language alongside theme and per-role configs.</done>
</task>

<task type="auto">
  <name>Task 2: Build canvas app i18n dictionary and replace all hardcoded Chinese text</name>
  <files>public/app.js, public/index.html, public/styles.css</files>
  <read_first>
    <file>public/app.js</file>
    <file>public/index.html</file>
    <file>public/styles.css</file>
  </read_first>
  <action>
1. In `public/app.js`, add a dictionary object and translation helper at the top of the file (after state initialization, before `init()`):
```js
const i18n = {
  zh: {
    "nav.workbench": "工作界面",
    "nav.history": "历史浏览器",
    "nav.home": "主页",
    "nav.sessions": "历史会话",
    "nav.settings": "设置",
    "settings.title": "API 设置",
    "settings.analysis": "分析",
    "settings.chat": "对话",
    "settings.image": "成图",
    "settings.endpoint": "API Endpoint",
    "settings.model": "Model",
    "settings.apiKey": "API Key",
    "settings.temperature": "Temperature",
    "settings.save": "保存",
    "settings.reset": "重置",
    "settings.darkMode": "深色模式",
    "settings.language": "语言",
    "source.uploadPrompt": "上传图片或文档",
    "source.uploadHint": "选择图片、Word、PDF、PPT 或 TXT，生成分支方向",
    "source.analyze": "分析",
    "source.urlPlaceholder": "https://...",
    "source.analyzeUrl": "分析链接",
    "chat.placeholder": "输入想继续探索的方向、风格或约束",
    "chat.send": "发送",
    "chat.attach": "上传图片或文本文件",
    "status.ready": "Ready",
    "status.busy": "Busy",
    "status.saved": "已保存",
    "status.saving": "保存中...",
    "status.error": "保存失败",
    "counts.label": "方向 {options} / 成图 {generated}",
    "option.generate": "生成这张图",
    "analysis.title": "图像理解",
    "analysis.eyebrow": "MODEL READ",
    "session.panelTitle": "历史会话",
    "session.close": "关闭",
    "session.export": "导出会话",
    "session.import": "导入会话",
    "session.save": "保存会话",
    "session.fit": "重置视图",
    "session.zoomOut": "缩小",
    "session.zoomIn": "放大",
    "health.checking": "checking",
    "health.demo": "demo",
    "health.api": "api",
    "health.mixed": "mixed",
    "chat.systemContext": "你是这个画布式图片生成应用里的创意对话助手。你的任务是帮助用户理解当前图片、比较分支方向、提出新的生成建议，或把用户的想法整理成可执行的视觉方向。回答用中文，保持简洁，通常 1-3 句。不要假装已经生成了新图片；如果用户想生成，请建议他点击方向节点或说明你会如何改提示词。",
    "chat.systemRole": "你是 Kimi K2.6 no thinking 模式下的创意对话助手。回答简洁、直接、可执行。",
    "analysis.systemPrompt": "你是一个视觉创意导演，正在为一个画布式图片生成应用分析用户上传的图片。请快速理解图片内容、主体、氛围、可延展的叙事方向，并给出 5 个不同的成图方向。这些方向会作为画布上的分支节点展示，用户点击后会调用成图模型。请只返回严格 JSON，不要 Markdown，不要代码块。",
    "generate.systemPrompt": "请基于参考图生成一张新图，保留原图最重要的主体、颜色关系或视觉记忆点，但不要只是复制。成图方向：{title}\n\n方向说明：{description}\n\n详细提示词：{prompt}\n\n输出应是一张完整、可独立展示的图片；构图清晰；不要添加水印、UI 截图边框或说明文字。",
    "explain.systemContext": "你是一位视觉创意评论助手，正在为画布式图片生成应用中的每张生成图撰写简短的内容讲解。用户会看到：原图分析摘要、选中的创作方向、以及实际发给成图模型的提示词。你的任务是用 1-2 句话（30-60 字）描述这张生成图在视觉上做了什么、保留了什么、改变了什么。语气专业、简洁、有画面感。不要重复提示词原文，要提炼成观众能感知的视觉描述。",
    "explain.systemRole": "你是 Kimi K2.6 no thinking 模式下的视觉创意评论助手。讲解要短、有画面感、不提技术细节。"
  },
  en: {
    "nav.workbench": "Workbench",
    "nav.history": "History",
    "nav.home": "Home",
    "nav.sessions": "Sessions",
    "nav.settings": "Settings",
    "settings.title": "API Settings",
    "settings.analysis": "Analysis",
    "settings.chat": "Chat",
    "settings.image": "Image",
    "settings.endpoint": "API Endpoint",
    "settings.model": "Model",
    "settings.apiKey": "API Key",
    "settings.temperature": "Temperature",
    "settings.save": "Save",
    "settings.reset": "Reset",
    "settings.darkMode": "Dark Mode",
    "settings.language": "Language",
    "source.uploadPrompt": "Upload image or document",
    "source.uploadHint": "Select image, Word, PDF, PPT or TXT to generate branches",
    "source.analyze": "Analyze",
    "source.urlPlaceholder": "https://...",
    "source.analyzeUrl": "Analyze Link",
    "chat.placeholder": "Enter direction, style or constraint to explore",
    "chat.send": "Send",
    "chat.attach": "Upload image or text file",
    "status.ready": "Ready",
    "status.busy": "Busy",
    "status.saved": "Saved",
    "status.saving": "Saving...",
    "status.error": "Save failed",
    "counts.label": "Options {options} / Generated {generated}",
    "option.generate": "Generate this image",
    "analysis.title": "Image Understanding",
    "analysis.eyebrow": "MODEL READ",
    "session.panelTitle": "Sessions",
    "session.close": "Close",
    "session.export": "Export",
    "session.import": "Import",
    "session.save": "Save",
    "session.fit": "Reset View",
    "session.zoomOut": "Zoom Out",
    "session.zoomIn": "Zoom In",
    "health.checking": "checking",
    "health.demo": "demo",
    "health.api": "api",
    "health.mixed": "mixed",
    "chat.systemContext": "You are the creative dialogue assistant in this canvas-based image generation app. Your task is to help users understand the current image, compare branch directions, propose new generation ideas, or organize user thoughts into executable visual directions. Answer in English, keep it concise, usually 1-3 sentences. Do not pretend to have generated a new image; if the user wants to generate, suggest clicking a direction node or explain how you would modify the prompt.",
    "chat.systemRole": "You are the Kimi K2.6 no-thinking creative dialogue assistant. Answers are concise, direct, and actionable.",
    "analysis.systemPrompt": "You are a visual creative director analyzing user-uploaded images for a canvas-based image generation app. Quickly understand the image content, subjects, atmosphere, and extensible narrative directions, then provide 5 different image generation directions. These directions will be displayed as branch nodes on the canvas; users click them to invoke the image generation model. Return strict JSON only, no Markdown, no code blocks.",
    "generate.systemPrompt": "Generate a new image based on the reference image, preserving the most important subjects, color relationships, or visual memory points, but do not simply copy. Direction: {title}\n\nDescription: {description}\n\nDetailed prompt: {prompt}\n\nOutput should be a complete, standalone image; clear composition; no watermarks, UI screenshot borders, or explanatory text.",
    "explain.systemContext": "You are a visual creative commentary assistant writing short descriptions for each generated image in a canvas-based image generation app. The user sees: original image analysis summary, selected creative direction, and the actual prompt sent to the image generation model. Your task is to describe in 1-2 sentences (30-60 words) what this generated image did visually, what it preserved, and what it changed. Tone: professional, concise, evocative. Do not repeat the prompt verbatim; distill it into a description the viewer can perceive.",
    "explain.systemRole": "You are the Kimi K2.6 no-thinking visual creative commentary assistant. Descriptions are short, evocative, and avoid technical details."
  }
};

let currentLang = "zh";

function t(key, vars = {}) {
  const dict = i18n[currentLang] || i18n.zh;
  let text = dict[key] || i18n.zh[key] || key;
  for (const [k, v] of Object.entries(vars)) {
    text = text.replace(new RegExp(`\\{${k}\\}`, "g"), String(v));
  }
  return text;
}

function setLanguage(lang) {
  if (lang !== "zh" && lang !== "en") return;
  currentLang = lang;
  document.documentElement.setAttribute("lang", lang === "zh" ? "zh-CN" : "en");
  localStorage.setItem("oryzae-lang", lang);
  renderAllText();
}

async function loadLanguage() {
  try {
    const res = await fetch("/api/settings");
    const data = await res.json();
    if (data.language === "zh" || data.language === "en") {
      setLanguage(data.language);
    }
  } catch (e) {
    console.error("Failed to load language", e);
  }
}

async function saveLanguage(lang) {
  setLanguage(lang);
  try {
    await fetch("/api/settings", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ language: lang })
    });
  } catch (e) {
    console.error("Failed to save language", e);
  }
}
```

2. Implement `renderAllText()` in `public/app.js`. This function updates all known DOM elements that contain user-facing text. Use `textContent` for labels and `placeholder` for inputs. Do NOT rebuild the DOM; only update text properties:
```js
function renderAllText() {
  // Nav labels
  const navLabels = {
    "nav.workbench": ".nav-link[href='/'] .nav-label",
    "nav.history": ".nav-link[href='/history/'] .nav-label",
    "nav.home": ".nav-link[href='/home.html'] .nav-label",
    "nav.sessions": "#historyToggle .nav-label",
    "nav.settings": ".nav-settings-label"
  };
  for (const [key, selector] of Object.entries(navLabels)) {
    const el = document.querySelector(selector);
    if (el) el.textContent = t(key);
  }

  // Source node
  const emptyStateStrong = document.querySelector("#emptyState strong");
  if (emptyStateStrong) emptyStateStrong.textContent = t("source.uploadPrompt");
  const emptyStateSpan = document.querySelector("#emptyState span");
  if (emptyStateSpan) emptyStateSpan.textContent = t("source.uploadHint");
  const analyzeButton = document.querySelector("#analyzeButton");
  if (analyzeButton) analyzeButton.textContent = t("source.analyze");
  const urlInput = document.querySelector("#urlInput");
  if (urlInput) urlInput.placeholder = t("source.urlPlaceholder");
  const urlAnalyzeButton = document.querySelector("#urlAnalyzeButton");
  if (urlAnalyzeButton) urlAnalyzeButton.textContent = t("source.analyzeUrl");

  // Chat
  const chatInput = document.querySelector("#chatInput");
  if (chatInput) chatInput.placeholder = t("chat.placeholder");
  const chatSendButton = document.querySelector("#chatSendButton");
  if (chatSendButton) chatSendButton.textContent = t("chat.send");
  const chatAttachButton = document.querySelector("#chatAttachButton");
  if (chatAttachButton) chatAttachButton.title = t("chat.attach");

  // Settings panel
  const settingsPanelTitle = document.querySelector(".settings-panel-header span");
  if (settingsPanelTitle) settingsPanelTitle.textContent = t("settings.title");
  const settingsTabs = document.querySelectorAll(".settings-tab");
  settingsTabs.forEach(tab => {
    const role = tab.dataset.role;
    if (role === "analysis") tab.textContent = t("settings.analysis");
    if (role === "chat") tab.textContent = t("settings.chat");
    if (role === "image") tab.textContent = t("settings.image");
  });
  const settingsLabels = document.querySelectorAll(".settings-form label span");
  if (settingsLabels[0]) settingsLabels[0].textContent = t("settings.endpoint");
  if (settingsLabels[1]) settingsLabels[1].textContent = t("settings.model");
  if (settingsLabels[2]) settingsLabels[2].textContent = t("settings.apiKey");
  if (settingsLabels[3]) settingsLabels[3].textContent = t("settings.temperature");
  const saveBtn = document.querySelector("#settingsForm .primary-button");
  if (saveBtn) saveBtn.textContent = t("settings.save");
  const resetBtn = document.querySelector("#settingsResetBtn");
  if (resetBtn) resetBtn.textContent = t("settings.reset");
  const darkModeLabel = document.querySelector(".settings-toggle-row span");
  if (darkModeLabel) darkModeLabel.textContent = t("settings.darkMode");
  const langLabel = document.querySelector(".settings-language-row span");
  if (langLabel) langLabel.textContent = t("settings.language");

  // Session panel
  const sessionPanelHeader = document.querySelector(".session-panel-header span");
  if (sessionPanelHeader) sessionPanelHeader.textContent = t("session.panelTitle");
  const closeSessionPanel = document.querySelector("#closeSessionPanel");
  if (closeSessionPanel) closeSessionPanel.setAttribute("aria-label", t("session.close"));

  // Buttons in nav-settings-grid
  const btnTitles = {
    "session.export": "#exportBtn",
    "session.import": "#importBtn",
    "session.save": "#saveButton",
    "session.fit": "#fitButton",
    "session.zoomOut": "#zoomOutButton",
    "session.zoomIn": "#zoomInButton"
  };
  for (const [key, selector] of Object.entries(btnTitles)) {
    const el = document.querySelector(selector);
    if (el) el.title = t(key);
  }

  // Analysis node
  const analysisEyebrow = document.querySelector("#analysisNode .eyebrow");
  if (analysisEyebrow) analysisEyebrow.textContent = t("analysis.eyebrow");
  const analysisTitle = document.querySelector("#analysisNode h2");
  if (analysisTitle) analysisTitle.textContent = t("analysis.title");

  // Option template
  const optionTemplate = document.querySelector("#optionTemplate");
  if (optionTemplate) {
    const genBtn = optionTemplate.content.querySelector(".generate-button");
    if (genBtn) genBtn.textContent = t("option.generate");
  }

  // Status
  updateStatusText();
}

function updateStatusText() {
  const statusTextEl = document.querySelector("#statusText");
  if (!statusTextEl) return;
  const current = statusTextEl.textContent;
  // Map current English statuses back to keys
  const keyMap = {
    "Ready": "status.ready",
    "Busy": "status.busy",
    "Saved": "status.saved",
    "Saving...": "status.saving",
    "Save failed": "status.error"
  };
  const key = keyMap[current] || "status.ready";
  statusTextEl.textContent = t(key);
}
```

3. Replace all hardcoded Chinese strings in `public/app.js` with `t("key")` calls. Specifically:
   - In `handleChat()`: replace the system context string with `t("chat.systemContext")` and system role with `t("chat.systemRole")`.
   - In `handleAnalyze()`: replace the analysis prompt with `t("analysis.systemPrompt")`.
   - In `handleGenerate()`: replace the generation prompt with `t("generate.systemPrompt", { title: option.title, description: option.description, prompt: option.prompt })`.
   - In `handleExplain()`: replace the system context with `t("explain.systemContext")` and system role with `t("explain.systemRole")`.
   - In `setStatus()`: ensure it calls `updateStatusText()` after setting the text, or use `t()` directly when setting.
   - In `updateCounts()`: use `t("counts.label", { options, generated })`.
   - In `buildDemoAnalysis()`: translate the demo analysis title, summary, detectedSubjects, moodKeywords, and option titles/descriptions/prompts using `t()` with keys like `"demo.title"`, `"demo.summary"`, `"demo.subjects"` (array), etc. Add these keys to the dictionary.

4. In `public/index.html`, replace hardcoded Chinese text with `data-i18n` attributes so `renderAllText()` can also use them as a fallback, OR simply keep the HTML as-is since `renderAllText()` overwrites the text on init. Add an inline script in `<head>` to set the initial language before render:
```html
<script>
  (function() {
    var l = localStorage.getItem("oryzae-lang");
    if (l === "zh" || l === "en") {
      document.documentElement.setAttribute("lang", l === "zh" ? "zh-CN" : "en");
    }
  })();
</script>
```

5. In the settings panel form (already added in 07-01/07-02), add a language selector:
```html
<div class="settings-language-row">
  <span>语言</span>
  <select id="languageSelect">
    <option value="zh">中文</option>
    <option value="en">English</option>
  </select>
</div>
```
Style it to match the form inputs. Wire it in `public/app.js`:
```js
const languageSelect = document.querySelector("#languageSelect");
if (languageSelect) {
  languageSelect.value = currentLang;
  languageSelect.addEventListener("change", () => {
    saveLanguage(languageSelect.value);
  });
}
```
Call `loadLanguage()` in `init()` after `loadTheme()`.
  </action>
  <verify>
    <automated>grep -n "function t(" public/app.js && grep -n "setLanguage\|loadLanguage\|saveLanguage" public/app.js && grep -n "data-i18n\|lang=" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `public/app.js` contains `const i18n = { zh: { ... }, en: { ... } };` with at least 30 keys in each language.
    - `public/app.js` contains `function t(key, vars = {})` and `function setLanguage(lang)`.
    - `public/app.js` contains `async function loadLanguage()` and `async function saveLanguage(lang)`.
    - `public/app.js` contains `renderAllText()` and calls it from `setLanguage()`.
    - `public/index.html` contains a `<script>` block with `localStorage.getItem("oryzae-lang")` and `document.documentElement.setAttribute("lang"`.
    - `public/index.html` contains `id="languageSelect"` with `<option value="zh">` and `<option value="en">`.
  </acceptance_criteria>
  <done>Canvas app has a complete i18n dictionary for zh/en, a t() helper, renderAllText() that updates all known UI labels, all system prompts and hardcoded strings replaced with dictionary lookups, and a language selector in the settings panel.</done>
</task>

<task type="auto">
  <name>Task 3: Build React history browser i18n and replace all hardcoded Chinese text</name>
  <files>app/src/lib/i18n.ts, app/src/components/cabinet/FileCabinet.tsx, app/src/components/cabinet/HistoryPage.tsx, app/src/components/cabinet/Sidebar.tsx, app/src/components/cabinet/SidebarItem.tsx, app/src/components/cabinet/AssetDetailPane.tsx, app/src/components/cabinet/AssetSidebar.tsx, app/src/components/cabinet/ContentArea.tsx, app/src/components/cabinet/FolderTab.tsx, app/src/components/share/ShareViewerPage.tsx, app/index.html</files>
  <read_first>
    <file>app/src/main.tsx</file>
    <file>app/src/components/cabinet/FileCabinet.tsx</file>
    <file>app/src/components/cabinet/HistoryPage.tsx</file>
    <file>app/src/components/cabinet/Sidebar.tsx</file>
    <file>app/src/components/cabinet/AssetDetailPane.tsx</file>
    <file>app/src/components/share/ShareViewerPage.tsx</file>
  </read_first>
  <action>
1. Create `app/src/lib/i18n.ts`:
```typescript
import { createContext, useContext, useState, useEffect, useCallback, type ReactNode } from "react";

export type Lang = "zh" | "en";

const dictionaries: Record<Lang, Record<string, string>> = {
  zh: {
    "app.title": "ORYZAE / History",
    "sidebar.sessions": "历史会话",
    "sidebar.assets": "资源",
    "sidebar.create": "新建",
    "detail.generatedImage": "生成图片",
    "detail.source": "来源",
    "detail.uploadedAt": "上传时间",
    "detail.generatedAt": "生成时间",
    "detail.explanation": "AI 讲解",
    "detail.noExplanation": "暂无讲解",
    "detail.prompt": "提示词",
    "detail.noPrompt": "暂无提示词",
    "detail.copy": "复制",
    "detail.download": "下载",
    "detail.close": "关闭",
    "share.title": "分享会话",
    "share.copyLink": "复制链接",
    "share.copied": "已复制",
    "share.expires": "有效期",
    "share.viewOnly": "只读视图",
    "folder.notes": "笔记",
    "folder.projects": "项目",
    "folder.archive": "归档",
    "history.loading": "加载中...",
    "history.error": "加载失败",
    "history.retry": "重试",
    "history.empty": "暂无会话",
    "history.nodeCount": "节点",
    "history.assetCount": "资源",
    "settings.darkMode": "深色模式",
    "settings.language": "语言",
    "lang.zh": "中文",
    "lang.en": "English"
  },
  en: {
    "app.title": "ORYZAE / History",
    "sidebar.sessions": "Sessions",
    "sidebar.assets": "Assets",
    "sidebar.create": "New",
    "detail.generatedImage": "Generated Image",
    "detail.source": "Source",
    "detail.uploadedAt": "Uploaded",
    "detail.generatedAt": "Generated",
    "detail.explanation": "AI Explanation",
    "detail.noExplanation": "No explanation",
    "detail.prompt": "Prompt",
    "detail.noPrompt": "No prompt",
    "detail.copy": "Copy",
    "detail.download": "Download",
    "detail.close": "Close",
    "share.title": "Shared Session",
    "share.copyLink": "Copy Link",
    "share.copied": "Copied",
    "share.expires": "Expires",
    "share.viewOnly": "View-only",
    "folder.notes": "Notes",
    "folder.projects": "Projects",
    "folder.archive": "Archive",
    "history.loading": "Loading...",
    "history.error": "Failed to load",
    "history.retry": "Retry",
    "history.empty": "No sessions",
    "history.nodeCount": "Nodes",
    "history.assetCount": "Assets",
    "settings.darkMode": "Dark Mode",
    "settings.language": "Language",
    "lang.zh": "中文",
    "lang.en": "English"
  }
};

function getDict(lang: Lang) {
  return dictionaries[lang] || dictionaries.zh;
}

interface I18nContextValue {
  lang: Lang;
  t: (key: string, vars?: Record<string, string | number>) => string;
  setLang: (lang: Lang) => void;
}

const I18nContext = createContext<I18nContextValue | null>(null);

export function I18nProvider({ children, initialLang = "zh" }: { children: ReactNode; initialLang?: Lang }) {
  const [lang, setLangState] = useState<Lang>(initialLang);

  const setLang = useCallback((next: Lang) => {
    setLangState(next);
    document.documentElement.setAttribute("lang", next === "zh" ? "zh-CN" : "en");
    localStorage.setItem("oryzae-lang", next);
  }, []);

  const t = useCallback(
    (key: string, vars: Record<string, string | number> = {}) => {
      const dict = getDict(lang);
      let text = dict[key] || getDict("zh")[key] || key;
      for (const [k, v] of Object.entries(vars)) {
        text = text.replace(new RegExp(`\\{${k}\\}`, "g"), String(v));
      }
      return text;
    },
    [lang]
  );

  useEffect(() => {
    function onStorage(e: StorageEvent) {
      if (e.key === "oryzae-lang") {
        const next = e.newValue as Lang;
        if (next === "zh" || next === "en") {
          setLangState(next);
          document.documentElement.setAttribute("lang", next === "zh" ? "zh-CN" : "en");
        }
      }
    }
    window.addEventListener("storage", onStorage);
    return () => window.removeEventListener("storage", onStorage);
  }, []);

  return <I18nContext.Provider value={{ lang, t, setLang }}>{children}</I18nContext.Provider>;
}

export function useI18n() {
  const ctx = useContext(I18nContext);
  if (!ctx) throw new Error("useI18n must be used within I18nProvider");
  return ctx;
}
```

2. In `app/src/main.tsx`, wrap `<App />` with `<I18nProvider>`:
```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";
import { I18nProvider } from "./lib/i18n";

const savedLang = localStorage.getItem("oryzae-lang") as "zh" | "en" | null;

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <I18nProvider initialLang={savedLang || "zh"}>
      <App />
    </I18nProvider>
  </React.StrictMode>
);
```

3. In `app/src/components/cabinet/FileCabinet.tsx`, add a language toggle alongside the theme toggle (added in 07-02). Use the `useI18n` hook:
```tsx
import { useI18n } from "@/lib/i18n";
// inside component:
const { lang, setLang, t } = useI18n();
// render:
<select value={lang} onChange={(e) => {
  const next = e.target.value as "zh" | "en";
  setLang(next);
  fetch("/api/settings", {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ language: next })
  }).catch(console.error);
}}>
  <option value="zh">{t("lang.zh")}</option>
  <option value="en">{t("lang.en")}</option>
</select>
```

4. Replace all hardcoded Chinese text in React components with `t("key")`:
   - `HistoryPage.tsx`: "历史会话" → `t("sidebar.sessions")`, loading/error/empty states.
   - `Sidebar.tsx`: use `t()` for any hardcoded labels if title prop is not already passed; ensure callers pass translated titles.
   - `SidebarItem.tsx`: no changes needed if it only renders props.
   - `AssetDetailPane.tsx`: all labels like "生成图片", "来源", "上传时间", "AI 讲解", "提示词", "复制", "下载", "关闭".
   - `AssetSidebar.tsx`: any hardcoded labels.
   - `ContentArea.tsx`: any hardcoded labels.
   - `FolderTab.tsx`: "笔记", "项目", "归档".
   - `ShareViewerPage.tsx`: "分享会话", "复制链接", "只读视图", etc.
   - Also update `app/index.html` `<title>` to be static "ORYZAE / History" (already English) or make it dynamic if desired; static is acceptable.

5. In `app/index.html`, add an inline script to set `lang` attribute before React hydrates:
```html
<script>
  (function() {
    var l = localStorage.getItem("oryzae-lang");
    if (l === "zh" || l === "en") {
      document.documentElement.setAttribute("lang", l === "zh" ? "zh-CN" : "en");
    }
  })();
</script>
```

6. Ensure `app/src/share-main.tsx` (if it exists and renders `ShareViewerPage`) also wraps with `I18nProvider` using the same pattern as `main.tsx`.
  </action>
  <verify>
    <automated>grep -n "useI18n\|I18nProvider" app/src/main.tsx app/src/components/cabinet/FileCabinet.tsx app/src/share-main.tsx && grep -n "t(" app/src/components/cabinet/AssetDetailPane.tsx app/src/components/cabinet/HistoryPage.tsx app/src/components/share/ShareViewerPage.tsx && grep -n "lang=" app/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/lib/i18n.ts` exports `useI18n` and `I18nProvider` and contains `const dictionaries: Record<Lang, Record<string, string>>` with keys for both `zh` and `en`.
    - `app/src/main.tsx` contains `<I18nProvider initialLang={savedLang || "zh"}>` wrapping `<App />`.
    - `app/src/components/cabinet/FileCabinet.tsx` imports `useI18n` from `"@/lib/i18n"` and renders a `<select>` with `onChange` that calls `setLang` and `fetch("/api/settings", { method: "PUT", body: JSON.stringify({ language:`.
    - `app/src/components/cabinet/HistoryPage.tsx` contains `t("sidebar.sessions")` or similar `t(` calls replacing all hardcoded Chinese strings.
    - `app/src/components/cabinet/AssetDetailPane.tsx` contains `t("detail.generatedImage")` or similar `t(` calls replacing all hardcoded Chinese strings.
    - `app/src/components/share/ShareViewerPage.tsx` contains `t("share.title")` or similar `t(` calls replacing all hardcoded Chinese strings.
    - `app/index.html` contains a `<script>` block with `localStorage.getItem("oryzae-lang")` and `document.documentElement.setAttribute("lang"`.
  </acceptance_criteria>
  <done>React history browser has a complete i18n module (app/src/lib/i18n.ts) with dictionary, useI18n hook, and I18nProvider wrapping the app. All hardcoded Chinese text in cabinet and share components is replaced with t() calls. Language toggle exists in FileCabinet and syncs to server and localStorage. Cross-tab sync works via storage events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server | Language value is user-controlled but cosmetic only |
| localStorage -> render | Anti-FOUC script trusts localStorage value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | localStorage "oryzae-lang" | accept | Value is cosmetic only; invalid values fall back to "zh" |
| T-07-10 | Information Disclosure | Language preference in localStorage | accept | Not sensitive data |
| T-07-11 | Denial of Service | Rapid language toggling | accept | No backend impact; purely client-side text updates |
</threat_model>

<verification>
- [ ] GET /api/settings returns top-level language field ("zh" or "en")
- [ ] PUT /api/settings with { language: "en" } persists and returns updated value
- [ ] Canvas app loads with correct language from localStorage without flash
- [ ] History browser loads with correct language from localStorage without flash
- [ ] Switching language in canvas app updates all visible text immediately
- [ ] Switching language in history browser updates all visible text immediately
- [ ] With both apps open, switching language in one updates the other within 1 second
- [ ] After refresh, both apps retain the selected language
- [ ] All system prompts sent to AI APIs match the selected language (Chinese prompts for zh, English for en)
- [ ] No hardcoded Chinese text remains in public/app.js or app/src/components/cabinet/ (verified by grep)
</verification>

<success_criteria>
- Settings schema and API support a global language field
- Canvas app has a complete zh/en dictionary, t() helper, and renderAllText() updater
- All hardcoded Chinese strings in public/app.js replaced with t() calls
- React history browser has I18nProvider, useI18n hook, and dictionary
- All hardcoded Chinese strings in app/src/components/ replaced with t() calls
- Language toggle exists in both canvas app settings panel and React history browser
- Language persists to database and localStorage; survives refresh and cross-tab navigation
- AI system prompts are language-aware (Chinese for zh, English for en)
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-personalization/07-03-SUMMARY.md`
</output>
