---
phase: 06-multi-format-input
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - app/src/components/cabinet/AssetSidebar.tsx
  - app/src/components/cabinet/AssetDetailPane.tsx
  - app/src/components/cabinet/HistoryPage.tsx
  - app/src/types/index.ts
  - src/api/sessions.js
  - public/app.js
  - app/src/components/cabinet/NodeGraphThumbnail.tsx
autonomous: true
requirements:
  - INPT-01
  - INPT-02
must_haves:
  truths:
    - "History browser sidebar shows text files in Files group with document icon and filename"
    - "History browser sidebar shows web links in Links group with external-link icon and domain"
    - "History browser detail pane shows text file metadata (filename, mime type, size, hash)"
    - "History browser detail pane shows web link metadata (URL, description, clickable link)"
    - "Source badges (filename/domain) are visible in NodeGraphThumbnail labels"
    - "Sessions with text or URL sources export/import correctly"
  artifacts:
    - path: "app/src/components/cabinet/AssetSidebar.tsx"
      provides: "Sidebar groups for images, files, links, chat with correct icons"
      contains: "getAssetIcon|transformSessionToItems"
    - path: "app/src/components/cabinet/AssetDetailPane.tsx"
      provides: "Detail views for image, file, link, chat assets"
      contains: "FileAssetDetail|LinkAssetDetail"
    - path: "app/src/types/index.ts"
      provides: "Type definitions for Asset, Node, SessionDetail"
      contains: "Asset|Node|SessionDetail"
    - path: "src/api/sessions.js"
      provides: "Serialization of sourceType and sourceUrl in session state"
      contains: "serializeState|sourceType|sourceUrl"
  key_links:
    - from: "src/api/sessions.js serializeState"
      to: "Node.data.sourceType"
      via: "node.data includes sourceType for source nodes"
      pattern: "sourceType"
    - from: "app/src/components/cabinet/AssetSidebar.tsx"
      to: "session.assets / session.nodes"
      via: "filters assets by kind and nodes by referenceUrl"
      pattern: "session.assets.filter|session.nodes.filter"
    - from: "app/src/components/cabinet/AssetDetailPane.tsx"
      to: "session.assets / session.nodes"
      via: "looks up asset by id or node by id"
      pattern: "session.assets.find|session.nodes.find"
---

<objective>
Integrate text file and web link sources into the history browser UI.

Purpose: Ensure the React-based file-cabinet history browser correctly displays, groups, and details text files and web links alongside images and chat messages.
Output: Updated AssetSidebar, AssetDetailPane, and type definitions with full support for text/link sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/components/cabinet/AssetSidebar.tsx
@app/src/components/cabinet/AssetDetailPane.tsx
@app/src/components/cabinet/HistoryPage.tsx
@app/src/types/index.ts
@src/api/sessions.js
@prisma/schema.prisma

## Interfaces

From app/src/types/index.ts:
```typescript
export interface Asset {
  id: string;
  sessionId: string;
  hash: string;
  kind: "upload" | "generated";
  mimeType: string;
  fileSize: number;
  fileName: string | null;
  createdAt: string;
}

export interface Node {
  id: string;
  sessionId: string;
  nodeId: string;
  type: string;
  x: number;
  y: number;
  width: number;
  height: number;
  data: any;
  collapsed: boolean;
  createdAt: string;
}

export interface SessionDetail {
  id: string;
  title: string;
  isDemo: boolean;
  createdAt: string;
  updatedAt: string;
  nodeCount: number;
  assetCount: number;
  viewState: object | null;
  nodes: Node[];
  links: Link[];
  assets: Asset[];
  chatMessages: ChatMessage[];
}
```

From app/src/components/cabinet/AssetSidebar.tsx:
```typescript
function getAssetIcon(assetType: "image" | "file" | "link" | "chat"): React.ReactNode
function transformSessionToItems(session: SessionDetail): SidebarItemData[]
```

From app/src/components/cabinet/AssetDetailPane.tsx:
```typescript
function FileAssetDetail({ asset }: { asset: Asset })
function LinkAssetDetail({ node }: { node: Node })
```

From src/api/sessions.js serializeState:
```javascript
// source node data:
{ fileName: "...", imageHash: "...", sourceType: "image" | "text" | "url" }
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update type definitions and session serialization for source types</name>
  <files>app/src/types/index.ts, src/api/sessions.js</files>
  <read_first>
    - app/src/types/index.ts (read Asset, Node, SessionDetail interfaces)
    - src/api/sessions.js (read serializeState, collectSourceAsset, and any existing source node serialization)
  </read_first>
  <action>
1. In `app/src/types/index.ts`:
   - Update `Asset` interface: `kind` should remain `"upload" | "generated"`, but add a comment that upload assets may have non-image mimeTypes (e.g., `application/pdf`, `text/plain`).
   - Add `sourceType` field to `Node.data` type documentation (optional — since `data` is `any`, no strict type change needed, but document the shape).

2. In `src/api/sessions.js`:
   - Update `serializeState` to include `sourceType` and `sourceUrl` in the source node data:
     ```javascript
     n.id === "source"
       ? {
           fileName: state.fileName || null,
           imageHash: state.sourceImageHash || extractAssetHash(state.sourceImage) || null,
           sourceType: state.sourceType || "image",
           sourceUrl: state.sourceUrl || null,
           sourceText: state.sourceText || null
         }
       : ...
     ```
   - Ensure `collectSourceAsset` handles text files correctly: when `sourceType === "text"`, the file buffer should already be stored via `storeFile` in the frontend or backend. If the backend stores it during `/api/analyze-text`, the hash is already in assets. If not, ensure the text file data URL is stored similarly to images.
   - Update `loadSession` in `public/app.js` (handled in prior plans) to restore `sourceType`, `sourceUrl`.
  </action>
  <acceptance_criteria>
    - `grep -n "sourceType" src/api/sessions.js` returns at least one match
    - `grep -n "sourceUrl" src/api/sessions.js` returns at least one match
    - `grep -n "sourceText" src/api/sessions.js` returns at least one match
    - `grep -n "application/pdf\|text/plain" app/src/types/index.ts` returns at least one match
    - `grep -n "sourceType.*image.*text.*url" app/src/types/index.ts` returns at least one match
  </acceptance_criteria>
  <done>`serializeState` includes sourceType, sourceUrl, sourceText in source node data; types documented.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance AssetSidebar and AssetDetailPane for text/link sources</name>
  <files>app/src/components/cabinet/AssetSidebar.tsx, app/src/components/cabinet/AssetDetailPane.tsx</files>
  <read_first>
    - app/src/components/cabinet/AssetSidebar.tsx (read transformSessionToItems and getAssetIcon)
    - app/src/components/cabinet/AssetDetailPane.tsx (read FileAssetDetail, LinkAssetDetail, ImageAssetDetail)
    - app/src/types/index.ts (read Asset and Node interfaces)
  </read_first>
  <action>
Update `transformSessionToItems` in `AssetSidebar.tsx` to better handle text and link sources.

1. **Files group** (uploaded assets):
   - Current code already filters `session.assets.filter((a: Asset) => a.kind === "upload")`.
   - For text files, improve the title and summary:
     - If `asset.mimeType.startsWith("text/") || asset.mimeType === "application/pdf" || asset.mimeType === "application/vnd.openxmlformats-officedocument.wordprocessingml.document" || asset.mimeType === "application/vnd.openxmlformats-officedocument.presentationml.presentation"`, set title to `asset.fileName || "Document"` and summary to `${asset.mimeType} · ${formatBytes(asset.fileSize)}`.
   - Ensure icon is `getAssetIcon("file")` for all non-image uploads.

2. **Links group** (nodes with referenceUrl):
   - Current code already filters nodes with `referenceUrl`.
   - Improve title: instead of always "Web Link", use the domain from the URL:
     ```typescript
     const url = node.data.option.referenceUrl as string;
     const domain = new URL(url).hostname;
     ```
   - Set title to `domain` and summary to `url.length > 60 ? url.slice(0, 60) + "..." : url`.
   - Add error handling for invalid URLs (fallback to "Web Link").

3. **Images group** (generated assets):
   - No change needed.

4. **Chat group**:
   - No change needed.

5. **Ordering**:
   - Keep current order: Images, Files, Links, Chat.

Update `AssetDetailPane` to show richer details for text files and links.

1. **FileAssetDetail** (already exists):
   - Keep the existing File icon + metadata grid.
   - Add a "Preview" section for text files:
     - If `asset.mimeType.startsWith("text/")`, fetch the file content from `/api/assets/${asset.hash}?kind=upload` and display the first 2000 characters in a `<pre>` block with scroll.
     - Use `useEffect` + `fetch` to load content.
   - For non-text uploads (binary docs), keep current behavior (icon + metadata only).

2. **LinkAssetDetail** (already exists):
   - Keep the ExternalLink icon + clickable URL.
   - Add a "Description" section that shows `node.data.option.description` if available.
   - Add a "Title" section that shows `node.data.option.title` if available.
   - If the node has `node.data.summary` (from analysis), show it as an "AI Summary" block.

3. **ImageAssetDetail** (already exists):
   - No change needed.

4. **Auto-select logic in HistoryPage**:
   - Current `useEffect` auto-selects first image, then first file, then first node, then first message.
   - This is fine — text files will be selected if no images exist.
  </action>
  <acceptance_criteria>
    - `grep -n "transformSessionToItems" app/src/components/cabinet/AssetSidebar.tsx` returns at least one match
    - `grep -n "FileAssetDetail" app/src/components/cabinet/AssetDetailPane.tsx` returns at least one match
    - `grep -n "LinkAssetDetail" app/src/components/cabinet/AssetDetailPane.tsx` returns at least one match
    - `grep -n "application/pdf\|text/plain\|wordprocessingml\|presentationml" app/src/components/cabinet/AssetSidebar.tsx` returns at least one match
    - `grep -n "new URL" app/src/components/cabinet/AssetSidebar.tsx` returns at least one match
    - `grep -n "Preview" app/src/components/cabinet/AssetDetailPane.tsx` returns at least one match
    - `grep -n "AI Summary" app/src/components/cabinet/AssetDetailPane.tsx` returns at least one match
  </acceptance_criteria>
  <done>AssetSidebar shows text files with filename + mime type in Files group, links with domain in Links group. AssetDetailPane shows text preview for text files, richer metadata for links, no regressions for images.</done>
</task>

<task type="auto">
  <name>Task 3: Update NodeGraphThumbnail to show source type labels</name>
  <files>app/src/components/cabinet/NodeGraphThumbnail.tsx</files>
  <read_first>
    - app/src/components/cabinet/NodeGraphThumbnail.tsx (read getNodeLabel and source node rendering logic)
    - app/src/types/index.ts (read Node interface)
  </read_first>
  <action>
Update `NodeGraphThumbnail` to display source-type-aware labels.

1. In `getNodeLabel(node)`:
   - For `node.type === "source"`, check `node.data?.sourceType`:
     - If `"text"`, return `node.data?.fileName || "Document"`.
     - If `"url"`, return `node.data?.sourceUrl ? new URL(node.data.sourceUrl).hostname : "Link"`.
     - Otherwise, return "Source" (existing behavior).
   - Add try/catch around `new URL()` to handle invalid URLs gracefully.

2. No other changes needed — the thumbnail already renders all nodes and links correctly.
  </action>
  <acceptance_criteria>
    - `grep -n "getNodeLabel" app/src/components/cabinet/NodeGraphThumbnail.tsx` returns at least one match
    - `grep -n "sourceType" app/src/components/cabinet/NodeGraphThumbnail.tsx` returns at least one match
    - `grep -n "fileName" app/src/components/cabinet/NodeGraphThumbnail.tsx` returns at least one match
    - `grep -n "sourceUrl" app/src/components/cabinet/NodeGraphThumbnail.tsx` returns at least one match
    - `grep -n "new URL" app/src/components/cabinet/NodeGraphThumbnail.tsx` returns at least one match
  </acceptance_criteria>
  <done>NodeGraphThumbnail shows "Document" or domain name for source nodes instead of generic "Source".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| History browser -> /api/assets | User views file content via asset endpoint |
| History browser -> session.nodes data | Node data rendered as HTML (XSS risk if unsanitized) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Cross-Site Scripting | LinkAssetDetail renders node.data | mitigate | React escapes text content by default; do not use `dangerouslySetInnerHTML`. URLs use `<a href>` with `rel="noopener noreferrer"`. |
| T-06-11 | Information Disclosure | Text file preview in FileAssetDetail | accept | File content is user-owned; preview is limited to first 2000 chars. |
| T-06-12 | Denial of Service | Large text file fetch | mitigate | `/api/assets` returns raw file; browser `<pre>` scroll handles large content. No server parsing. |
</threat_model>

<verification>
- Open history browser for a session with a text file source: sidebar shows file in Files group, detail pane shows metadata + text preview.
- Open history browser for a session with a URL source: sidebar shows domain in Links group, detail pane shows clickable URL + description.
- NodeGraphThumbnail for text session shows filename instead of "Source".
- NodeGraphThumbnail for URL session shows domain instead of "Source".
- Export/import session with text/URL source: all data preserved.
</verification>

<success_criteria>
- [ ] History browser sidebar correctly groups text files under Files with document icon
- [ ] History browser sidebar correctly groups links under Links with domain name
- [ ] History browser detail pane shows text file metadata and preview
- [ ] History browser detail pane shows link metadata and clickable URL
- [ ] NodeGraphThumbnail labels reflect source type (filename for text, domain for URL)
- [ ] Sessions with text/URL sources export and import correctly
- [ ] No TypeScript errors in updated components
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-format-input/06-03-SUMMARY.md`
</output>
