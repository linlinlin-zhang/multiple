{
  "name": "thoughtgrid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "node scripts/build-low-resource.js",
    "build:app": "node scripts/build-low-resource.js",
    "build:fast": "npm --prefix app run build:fast",
    "start": "node server.js",
    "test": "node scripts/test-integration.js",
    "test:guards": "node scripts/test-chat-action-guard.js && node scripts/test-canvas-action-reliability.js",
    "test:db": "node scripts/test-db.js",
    "test:rag": "node scripts/test-rag.js",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "jszip": "^3.10.1",
    "pdf-parse": "^2.4.5",
    "pg": "^8.13.0",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
